{
  "name": "q10",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "start": "npx serve -s dist",
    "preview": "vite preview",
    "heroku-postbuild": "npm run build"
  },
  "devDependencies": {
    "vite": "^6.1.0"
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
